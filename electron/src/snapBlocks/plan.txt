MicroBlocks as Snap Blocks Experiment

To do:
[x] rework built-in block specs as object keyed by selector
[x] code to generate MicroBlocks code from blocks
[x] test parser
[x] handle variables ("var" slot type)
[x] set slot default values
[x] add MicroBit display input slot type
[x] fix: remove quotes from quoted strings (but may need for 'var' slots...)
[x] slot dropdown menu support
[x] optional block sections (see e.g. Snap! "broadcast" block)
[ ] fix: if-else blocks are expanded (don't extend args for option sections?)

[ ] BlockExpander morph
[ ] implement collapse

Block cleanups:
  - remove: comment support (comment slot, "allComments()"
  - remove Lisp support, MutiArgMorph support

multi-arg: look for labelText and slotSpec

Issues:
[x] Snap! seems to allow arg slots to contain null, MicroBlocks assumes they always have some value.
  (Solutions: MB always sets a default value OR MicroBlocks compiler can deal with "null" arg values.)

Tips from Bernat:
  - how get a Morphic inspector in Snap! (both MB Snap and actual Snap!)
	- shift-click on Snap logo to enter dev mode
	- click on world in SnapBlocks to enter dev mode

mb-block.js cleanups:
  - replace getVarNamesDict

Long term plan:
  [x] basic block mechanics for MicroBlocks blocks
  [x] ability to export MicroBlocks code from blocks of a single script
  [x] parser (medium)
  [x] block spec lookup mechanism
  [x] ability to parse a single script and generate blocks for it
  [x] project representation (medium)
  [x] ability to import an entire project
  [ ] ability to export an entire project
  [ ] compiler (big!)
  [ ] ability to print compiled code and bytecodes
  [ ] IDE communication protocol (huge!)
  [ ] ability to compile, download, and run a script
  [ ] ability to compile entire project
  [ ] library support
  [ ] ability to install VM's from the IDE (big!)
  [ ] decompiler (big!)
  - ...

------------------------
GP code that will need to ported:
	126		 ide/MicroBitDisplaySlot.gp (done!)

	1236	ide/MicroBlocksProject.gp

	1332 	ide/MicroBlocksCompiler.gp
	1102		ide/MicroBlocksDecompiler.gp

	3216	ide/MicroBlocksRuntime.gp
	265		ide/MicroBlocksExchange.gp
	164		ide/MicroBlocksFinder.gp

	1028	ide/ESPTool.gp -- use existing Javascript library?
	153		ide/MicroBlocksAPI.gp -- won't be needed; use direct Javascript calls

	// the following are mostly UI and other things that we'll do differently Javascript
	1266	ide/MicroBlocksEditor.gp
	1542	ide/MicroBlocksScripter.gp

	380		ide/MicroBlocksDataGraph.gp
	1071	ide/MicroBlocksFilePicker.gp
	637		ide/MicroBlocksLibraryWidgets.gp

	133		ide/MicroBlocksTipBar.gp
	456		ide/MicroBlocksHelp.gp

	295		ide/MicroBlocksSelection.gp
	237		ide/MicroBlocksFlasher.gp
	108		ide/MicroBlocksSpinner.gp

	17		ide/MicroBlocksPatches.gp
 	215		ide/MicroBlocksAppMaker.gp
	14979	total

