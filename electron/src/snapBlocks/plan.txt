MicroBlocks as Snap Blocks Experiment

To do:
[x] test script to generate MicroBlocks blocks
[x] make context menus work on blocks and the scripting area
[x] fix input slot types for MicroBlocks blocks (including C-shaped ones)
[x] blocks palette
[x] block specs for MicroBlocks built-in blocks
[x] make boolean slots default to true
[x] add category labels to palette
[x] make palette delete dropped morphs
[x] make boolean slots accept reporters of any type (how?)
[x] make "if" be expandable to multiple cases (how?)

[ ] rework built-in block specs as object keyed by selector
[ ] set slot default values 
[ ] option block sections (see e.g. Snap! "broadcast" block)
[ ] code to generate MicroBlocks code from blocks
[ ] test parser

Issues:
[ ] Snap! seems to allow arg slots to contain null, MicroBlocks assumes they always have some value. (Solutions: MB always sets a default value OR MicroBlocks compiler can deal with "null" arg values.)

Questions answered by Bernat:
  - how get a Morphic inspector in Snap! (both MB Snap and actual Snap!)
	- shift-click on Snap logo to enter dev mode
	- click on world in SnapBlocks to enter dev mode
  - where is native clipboard paste implemented? (see initKeyboardHandler)

Long term plan:
  - basic block mechanics for MicroBlocks blocks
  - ability to export MicroBlocks code from blocks of a single script
  - parser (medium!)
  - block spec lookup mechanism
  - ability to parse a single script and generate blocks for it
  - ability to import an entire project
  - ability to export an entire project
  - compiler (big!)
  - ability to print compiled code and bytecodes
  - IDE communication protocol (huge!)
  - ability to compile, download, and run a script
  - ability to compile entire project
  - library support
  - ability to install VM's from the IDE
  - decompiler (huge!)
  - ...

